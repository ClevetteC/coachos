# CoachOS Web — Claude Code

## Active task (do this first)

**Fix signup `email rate limit exceeded` for beta and production.**

Read and execute in order:

```
docs/CLAUDE_CODE_HANDOFF_AUTH_EMAIL.md
```

Supporting runbook: `docs/SUPABASE_AUTH_EMAIL.md`

Code changes are largely in the working tree; **Supabase custom SMTP (Resend) is still required** in the dashboard before testers are unblocked.

---

@AGENTS.md
